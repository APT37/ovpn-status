[package]
name = "ovpn_status"
version = "0.2.7"
edition = "2021"
license = "MIT"

[dependencies]
reqwest = { version = "0.12.*", default-features = false, features = ["blocking", "cookies", "http2", "json", "rustls-tls"] }
serde = { version = "1.0.*", features = ["derive"] }
colored = "2.1.*"
